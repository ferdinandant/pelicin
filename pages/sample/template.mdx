import { MetadataConfigurer } from '@pelicin/layout';
import { References } from '@pelicin/components';
import { smartTrim } from '@pelicin/utils';

<MetadataConfigurer title="Template" dateCreated="2021-02-06" />

# Template

Lorem Ipsum Dolor Sit Amet

<WebEditor
  html={smartTrim(`
    <div id="height-width-example">
      Hi!
    </div>    
  `)}
  css={smartTrim(`
    #height-width-example {
      width: 100px;
      height: 50px;
      background-color: #ccc;
    }    
  `)}
/>

<WebEditor
  hideHTML
  hideCSS
  html={smartTrim(`
  `)}
  css={smartTrim(`
  `)}
/>

<DocumentationList
  items={[
    {
      name: 'row',
      description: 'fill in each row in turn, adding new rows as necessary (default)',
    },
  ]}
/>

## References

<References
  references={[
    {
      text: 'MDN',
      link: `https`,
    },
  ]}
></References>
