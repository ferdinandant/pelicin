import { LoadSyntaxHighlighter } from '@pelicin/loaders';
import { CodeRunner } from '@pelicin/components';
import { smartTrim } from '@pelicin/utils';

<LoadSyntaxHighlighter />

# With Code

Default syntax highlighter:

```html
<!DOCTYPE html>
<html>
  <head>
    ...
  </head>
  <body>
    ...
  </body>
</html>
```

Code editor:

<CodeRunner
  language="js"
  code={smartTrim([
    `
      function printHello() {
        const message = 'Hello world!';
        console.log(message);
      }
    `,
    `
      printHello();
    `,
  ])}
/>
