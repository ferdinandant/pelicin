import { MetadataConfigurer } from '@pelicin/layout';
import { References, WebEditor } from '@pelicin/components';
import { smartTrim } from '@pelicin/utils';

<MetadataConfigurer title="Table Border" dateCreated="2021-03-11" />

# Table Border

Table borders have 2 additional CSS properties.

## Table Border Properties

### `border-collapse`

Sets whether cells inside a `<table>` have shared or separate borders. Specify `collapse` or `separate`.

<WebEditor
  hideHTML
  hideCSS
  html={smartTrim(`
    <table id="table-collapse">
      <caption><code>border-collapse: collapse</code></caption>
      <tr><td>1</td><td>2</td></tr>
      <tr><td>3</td><td>4</td></tr>
    </table>
    <table id="table-separate">
      <caption><code>border-collapse: separate</code></caption>
      <tr><td>1</td><td>2</td></tr>
      <tr><td>3</td><td>4</td></tr>
    </table>
  `)}
  css={smartTrim(`
    /* Set border-collapse property for the <table> */
    #table-collapse {
      border-collapse: collapse;
    }
    #table-collapse td {
      border: 1px solid blue;
    }
    #table-separate {
      border-collapse: separate;
      border-spacing: 10px;
    }
    #table-separate td {
      border: 1px solid red;
    } 
`)}
/>

### `border-spacing`

Sets the distance between table borders. (Only applies if you set `border-collapse: separate`.)

Can take up to 2 lengths: horizontal spacing, and vertical spacing.

## References

<References
  references={[
    {
      text: 'border-collapse (MDN)',
      link: `https://developer.mozilla.org/en-US/docs/Web/CSS/border-collapse`,
    },
    {
      text: 'border-spacing (MDN)',
      link: `https://developer.mozilla.org/en-US/docs/Web/CSS/border-spacing`,
    },
  ]}
></References>
