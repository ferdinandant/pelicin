import { References } from '@pelicin/components';
import { smartTrim } from '@pelicin/utils';

# Time

The time data type represents a time value.

## Time Units

CSS only uses these two time units:

- **`s`** &mdash; second (e.g. `0.25s`)
- **`ms`** &mdash; millisecond (e.g. `750ms`)

## References

<References
  references={[
    {
      text: '<time> (MDN)',
      link: `https://developer.mozilla.org/en-US/docs/Web/CSS/time`,
    },
  ]}
></References>
