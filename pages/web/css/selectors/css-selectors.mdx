import { MetadataConfigurer } from '@pelicin/layout';
import {
  References,
  Image,
  NoteBox,
  WebEditor,
  TodoBox,
  FixedColumnWidthTable,
} from '@pelicin/components';
import { smartTrim } from '@pelicin/utils';

<MetadataConfigurer title="CSS Selectors" dateCreated="2021-02-10" />

# CSS Selectors

<TodoBox />

<dfn>CSS selectors</dfn> select which HTML element(s) is affected by a group of CSS rules.

<FixedColumnWidthTable
  headers={[
    {
      key: 'syntax',
      text: 'Syntax',
    },
    {
      key: 'description',
      text: 'Description',
    },
  ]}
  contents={[]}
/>
