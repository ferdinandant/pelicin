import { MetadataConfigurer } from '@pelicin/layout';
import { References, HTMLTagsIndexTable, Badge, WebEditor } from '@pelicin/components';
import { smartTrim } from '@pelicin/utils';

export const Attribute = (props) => (
  <strong>
    <code>{props.children}</code>
  </strong>
);

# HTML Tags: The Basics

These are the most common HTML tags. You would put these inside `<body>`, since they correspond to the content of your web page.

- <dfn>Block-level elements</dfn> will be placed in their own line

- <dfn>Inline elements</dfn> will flow in-line with the surrounding text

<HTMLTagsIndexTable
  specialMapping={{
    div: 'div-and-span',
    span: 'div-and-span',
  }}
  tags={[
    {
      '@separator': 'Containers',
    },
    {
      tag: 'div',
      type: 'block',
      description: <>Creates a generic block-level container</>,
    },
    {
      tag: 'span',
      type: 'inline',
      description: <>Creates a generic inline container</>,
    },
    {
      '@separator': 'Common tags',
    },
    {
      tag: 'h1-h6',
      type: 'block',
      description: <>Marks headers</>,
    },
    {
      tag: 'p',
      type: 'block',
      description: <>Marks paragraphs</>,
    },
    {
      tag: 'hr',
      type: 'block',
      description: <>Inserts a horizontal ruler</>,
    },
    {
      tag: 'br',
      type: 'inline',
      description: <>Inserts a line break</>,
    },
    {
      tag: 'a',
      type: 'inline',
      description: (
        <>
          Creates <a href="#">anchors/hyperlinks</a>
        </>
      ),
    },
    {
      tag: 'b',
      type: 'inline',
      description: (
        <>
          Makes text <b>bold</b>
        </>
      ),
    },
    {
      tag: 'i',
      type: 'inline',
      description: (
        <>
          Makes text <i>italic</i>
        </>
      ),
    },
    {
      tag: 'u',
      type: 'inline',
      description: (
        <>
          Makes text <u>underlined</u>
        </>
      ),
    },
    {
      tag: 's',
      type: 'inline',
      description: (
        <>
          Renders text with <s>strikethrough</s>
        </>
      ),
    },
    {
      tag: 'sub',
      type: 'inline',
      description: (
        <>
          Renders text as <sub>subscript</sub>
        </>
      ),
    },
    {
      tag: 'sup',
      type: 'inline',
      description: (
        <>
          Render text as <sup>superscript</sup>
        </>
      ),
    },
  ]}
/>

## `<div>` and `<span>`

`<div>` is used to create a generic <dfn>block-level container</dfn>, which means it will take the whole horizontal space of its parent
(it will be on its new line).

`<span>` is used to create a generic <dfn>inline container</dfn>, which means it will flow with the current line of text.

These containers by themselves will not change how the content is displayed; you will normally use this for layouting and styling with CSS.

<WebEditor html={smartTrim([
  `
    <!-- div creates its own line -->
    <div style="background-color: red; color: white">
      I'm creating a new line!
    </div>
  `,
  `
    <!-- span flows inline with the surrounding text -->
    Lorem ipsum
    <span style="background-color: blue; color: white">
      I'm flowing with the text
    </span>
    dolor sit amet
  `,
])} />

## `<h1>` &ndash; `<h6>`

These are used to create <em>headers</em>. They are block-level elements.

- `<h1>` would normally be used for title
- `<h2>` would be for the subtitle
- `<h3>` would be for the sub-subtitle
- so on and so forth.

<WebEditor
  html={smartTrim(`
    <h1>H1: Nice title</h1>
    <h2>H2: Nice sub-title</h2>
    <h3>H3: Nice sub-sub-title</h3>
  `)}
/>

## `<p>`

Marks a paragraph. It's a block-level element.

<WebEditor
  html={smartTrim(`
    <p>Lorem ipsum dolor sit amet. Consectetur adipisicing elit.</p>
    <p>Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.</p>
  `)}
/>

## `<hr>`

Inserts a <dfn>horizontal ruler</dfn>.

<WebEditor
  html={smartTrim(`
    one two three
    <hr>
    one two three
  `)}
/>

## `<br>`

Inserts a <dfn>line break</dfn>.

<WebEditor
  html={smartTrim(`
    <p>
      Lorem ipsum
      <br />
      dolor sit amet
    </p>
  `)}
/>

## `<a>`

Creates <dfn>anchors</dfn> and hyperlinks.

<Badge color="blue">Common attributes</Badge>
<ul>
  <li>
    <Attribute>href=[...]</Attribute> &mdash; where the link points to
  </li>
  <li>
    <Attribute>target=[...]</Attribute> &mdash; where to open it (
    <a href="/web/html/attributes/html-attributes-recurring#target">read here</a>)
  </li>
</ul>

<Badge color="blue">Less common attributes</Badge>
<ul>
  <li>
    <Attribute>download=[...]</Attribute> &mdash; instead of opening the link, prompt the user to
    download it. If value is not empty, it will be the name of the downloaded file. (Only works for
    same-origin URLs, or <code>blob:</code> and <code>data:</code> schemes)
  </li>
  <li>
    <Attribute>ping=[...]</Attribute> &mdash; list of space-separated URLs to which the browser will
    send <code>PING</code> POST requests when the hyperlink is followed (usable for tracking).
  </li>
  <li>
    <Attribute>hreflang=[...]</Attribute> &mdash; indicates the language of the linked resource
    (e.g. <code>en-US</code>)
  </li>
  <li>
    <Attribute>rel=[...]</Attribute> &mdash; relation of the linked resource (
    <a href="/web/html/attributes/html-attributes-recurring#rel">read here</a>)
  </li>
  <li>
    <Attribute>referrerpolicy=[...]</Attribute> &mdash;{' '}
    <a href="/web/html/attributes/html-attributes-recurring#referrerpolicy"> read here</a>
  </li>
</ul>

<WebEditor html={smartTrim([
  `
    <p>To search the internet, <a href="https://google.com">click here</a>!</p>
  `,
  `
    <!-- Prompts download when clicked -->
    <p>
      Taken from MDN site:
      <a href="/res/web/html/mdx-info2.png" download>Download an image?</a>
    </p>
  `,
  `
    <!-- When clicked, the header will be brought to top of page -->
    <h2 id="my-anchor">My Anchor</h2>
    <p>Click <a href="#my-anchor">here</a> to go to My Anchor</p>
  `,
])} />

## `<b>`

Makes text <em>bold</em>.

<WebEditor
  html={smartTrim(`
    <b>I'm bold!</b>
  `)}
/>

## `<i>`

Makes text <em>italic</em>.

<WebEditor
  html={smartTrim(`
    <i>I'm italic!</i>
  `)}
/>

## `<u>`

Makes text <em>underlined</em>.

<WebEditor
  html={smartTrim(`
    <u>I'm underlined!</u>
  `)}
/>

## `<s>`

Renders text with <em>strikethrough</em>.

<WebEditor
  html={smartTrim(`
    <s>I'm struck-through!</s>
  `)}
/>

## `<sub>`

Renders text as <em>subscript</em>

<WebEditor
  html={smartTrim(`
    I'm a <sub>subscript</sub>
  `)}
/>

## `<sup>`

Renders text as <em>superscript</em>

<WebEditor
  html={smartTrim(`
    I'm a <sup>superscript</sup>
  `)}
/>

## References

<References
  references={[
    {
      text: 'HTML elements reference (MDN)',
      link: `https://developer.mozilla.org/en-US/docs/Web/HTML/Element`,
    },
  ]}
></References>
