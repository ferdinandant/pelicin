import { MetadataConfigurer } from '@pelicin/layout';
import { References, FixedColumnWidthTable, LightBox, NoteBox, Badge } from '@pelicin/components';

export const Attribute = (props) => (
  <strong>
    <code>{props.children}</code>
  </strong>
);

<MetadataConfigurer title="HTML Tags: Table" dateCreated="2021-01-22" />

# HTML Tags: Table

<NoteBox type="info">
  <p>
    The only essential things on this page are:
    <ul>
      <li>
        <code>{`<table>`}</code>, <code>{`<tr>`}</code>, <code>{`<th>`}</code>, <code>{`<td>`}</code>
      </li>
      <li>
        <code>{`colspan`}</code> and <code>{`rowspan`}</code> attributes
      </li>
    </ul>
  </p>
</NoteBox>

These are the HTML tags you use to make tables.

<FixedColumnWidthTable
  tableStyle={{
    fontSize: '0.9em',
  }}
  headers={[
    {
      text: 'Tag',
      key: 'tag',
      formatContent: (tag) => {
        const specialMapping = {};
        return (
          <>
            <a href={specialMapping[tag] ? `#${specialMapping[tag]}` : `#${tag}`}>
              <code>&lt;{tag}&gt;</code>
            </a>
          </>
        );
      },
      contentStyle: { verticalAlign: 'top' },
      width: 100,
    },
    {
      text: 'Type',
      key: 'type',
      formatContent: (type) => {
        return type === 'block' ? (
          <Badge color="red">Block</Badge>
        ) : type === 'inline' ? (
          <Badge color="blue">Inline</Badge>
        ) : (
          <Badge color="gray">Other</Badge>
        );
      },
      width: 80,
    },
    {
      text: 'Description',
      key: 'description',
    },
  ]}
  contents={[
    {
      tag: 'table',
      type: 'block',
      description: 'Represents table',
    },
    {
      tag: 'tr',
      type: 'other',
      description: 'Represents table row',
    },
    {
      tag: 'th',
      type: 'other',
      description: 'Represents table header cell',
    },
    {
      tag: 'td',
      type: 'other',
      description: 'Represents table data cell',
    },
  ]}
/>

## `<table>`

Represents table.

```html
<table>
  <tr>
    <th>No.</th>
    <th>Name</th>
    <th>Address</th>
  </tr>
  <tr>
    <td>1</td>
    <td>John Doe</td>
    <td>Sydney, Australia</td>
  </tr>
  <tr>
    <td>2</td>
    <td>Takara Tomy</td>
    <td>Kyushu, Japan</td>
  </tr>
</table>
```

<!-- prettier-ignore -->
<LightBox label="Preview">
  <table>
    <tr>
      <th>No.</th>
      <th>Name</th>
      <th>Address</th>
    </tr>
    <tr>
      <td>1</td>
      <td>John Doe</td>
      <td>Sydney, Australia</td>
    </tr>
    <tr>
      <td>2</td>
      <td>Takara Tomy</td>
      <td>Kyushu, Japan</td>
    </tr>
  </table>
</LightBox>

## `<tr>`

Represents <em>table row</em> (row of cells in table).

## `<th>`

Defines a <em>table header</em> cell.

<Badge color="blue">Common attributes</Badge>
<ul>
  <li>
    <Attribute>colspan=[...]</Attribute> &mdash; how many columns the cell extends to
  </li>
  <li>
    <Attribute>rowspan=[...]</Attribute> &mdash; how many rows the cell extends to
  </li>
</ul>

<Badge color="blue">Obscure attributes</Badge>
<ul>
  <li>
    <Attribute>abbr=[...]</Attribute> &mdash; short abbreviated description of the cell's content
    (e.g. for screen readers)
  </li>
  <li>
    <Attribute>scope=[...]</Attribute> &mdash; Can be:
    <ul>
      <li>
        <code>row</code> <i>(the header is associated with this row)</i>
      </li>
      <li>
        <code>col</code> <i>(the header is associated with this column)</i>
      </li>
      <li>
        <code>rowgroup</code> <i>(the header is associated with a group of rows)</i>
      </li>
      <li>
        <code>colgroup</code> <i>(the header is associated with a group of columns)</i>
      </li>
      <li>
        <code>auto</code> <i>(the default value)</i>
      </li>
    </ul>
  </li>
  <li>
    <Attribute>headers=[...]</Attribute> &mdash; assuming this cell has parent header cell(s), this
    is the space-separated <code>id</code> values of its <code>{`<th id="...">`}</code> parent
    header cell(s). See example below.
  </li>
</ul>

```html
<table>
  <tr>
    <th id="name" scope="colgroup" colspan="2">Name</th>
  </tr>
  <tr>
    <th scope="col" headers="name">First Name</th>
    <th scope="col" headers="name">Last Name</th>
  </tr>
</table>
```

<!-- prettier-ignore -->
<LightBox label="Preview">
  <tr>
    <th id="name" scope="colgroup" colspan="2">Name</th>
  </tr>
  <tr>
    <th scope="col" headers="name">First Name</th>
    <th scope="col" headers="name">Last Name</th>
  </tr>
</LightBox>

## References

<References
  references={[
    {
      text: 'HTML elements reference (MDN)',
      link: `https://developer.mozilla.org/en-US/docs/Web/HTML/Element`,
    },
    {
      text: '<th> (MDN)',
      link: `https://developer.mozilla.org/en-US/docs/Web/HTML/Element/th`,
    },
    {
      text: 'HTML <th> headers Attribute (W3Schools)',
      link: `https://www.w3schools.com/tags/tryit.asp?filename=tryhtml5_th_headers`,
    },
  ]}
></References>
