import { References, NoteBox, Badge } from '@pelicin/components';

# Explanation to Recurring HTML Attributes

Some HTML attributes are recurring in more than one type of elements. To save spaces for their (usually long) explanation, they are described here.

## `crossorigin=[...]`

Defines how the browser should make the request in terms of CORS. Possible values:

<ul>
  <li>
    <strong>
      <code>anonymous</code>
    </strong>{' '}
    &mdash; <em>Do not</em> send credentials for CORS when requesting the resource. Expect the
    server to allow CORS via <code>Access-Control-Allow-Origin</code> response header
  </li>
</ul>

<ul>
  <li>
    <strong>
      <code>use-credentials</code>
    </strong>{' '}
    &mdash; Send credentials for CORS when requesting the resource. Expect the server to allow CORS
    via <code>Access-Control-Allow-Credentials</code> response header
  </li>
</ul>

<strong>Note:</strong> <dfn>Credentials</dfn> are cookies, authorization headers, or TLS client certificates.

## `form=[...]`

Which `id` of `<form id="...">` this form item is associated with.

(Allows you to place the form item anywhere in the page, not necessarily as the children of said `<form>`.)

## `referrerpolicy=[...]`

Whether or not the browser should include the `Referer` header when making the request. Possible values:

<Badge color="red">No-referrer</Badge>
<ul>
  <li>
    <strong>
      <code>no-referrer</code>
    </strong>{' '}
    &mdash; Don't send referer
  </li>
</ul>
<ul>
  <li>
    <strong>
      <code>no-referrer-when-downgrade</code>
    </strong>{' '}
    &mdash; (This is default.) Don't send referer if we are making request to less secure protocol
    (i.e. HTTPS to HTTP). Otherwise, send origin, path, and querystring as <code>Referer</code>.
  </li>
</ul>

<Badge color="red">Origin</Badge>
<ul>
  <li>
    <strong>
      <code>origin</code>
    </strong>{' '}
    &mdash; Only send origin as <code>Referer</code>
  </li>
</ul>
<ul>
  <li>
    <strong>
      <code>same-origin</code>
    </strong>{' '}
    &mdash; Only send origin as <code>Referer</code> when making same-origin requests. Otherwise,
    don't send referer.
  </li>
</ul>
<ul>
  <li>
    <strong>
      <code>origin-when-cross-origin</code>
    </strong>{' '}
    &mdash; Only send origin as <code>Referer</code> when making cross-origin request. Otherwise,
    send origin, path, and querystring as <code>Referer</code>.
  </li>
</ul>

<Badge color="red">Strict origin</Badge>
<ul>
  <li>
    <strong>
      <code>strict-origin</code>
    </strong>{' '}
    &mdash; Only send origin as <code>Referer</code> when the protocol security level is at least
    the same. Otherwise, don't send referer.
  </li>
</ul>
<ul>
  <li>
    <strong>
      <code>strict-origin-when-cross-origin</code>
    </strong>{' '}
    &mdash; (1) Only send origin as <code>Referer</code> when making cross-origin request and the
    protocol security level is at least the same. (2) Send origin, path, and querystring as{' '}
    <code>Referer</code> when making same-origin request and the protocol security level is at least
    the same. (3) Otherwise, don't send referer.
  </li>
</ul>

<Badge color="red">Unsafe URL</Badge>
<ul>
  <li>
    <strong>
      <code>unsafe-url</code>
    </strong>{' '}
    &mdash; Send origin, path, and querystring as <code>Referer</code>.{' '}
    <em>(May leak private data over insecure HTTP.)</em>
  </li>
</ul>

## `rel=[...]`

Defines relation of the linked resource to the document. When specifying more than one, separate using space.

The possible values depend on how `rel` was used:

- `<link rel="...">`
- `<a rel="...">` or `<area rel="...">`,
- `<form rel="...">`

### `rel` of `<link>`

<Badge color="red">Link to other page</Badge>
<ul>
  <li>
    <strong>
      <code>author</code>
    </strong>{' '}
    &mdash; link to the author
  </li>
  <li>
    <strong>
      <code>canonical</code>
    </strong>{' '}
    &mdash; link to the preferred URL for this document
  </li>
  <li>
    <strong>
      <code>alternate</code>
    </strong>{' '}
    &mdash; link to the alternate representation of this document, e.g.
    <ul>
      <li>
        <code>{`<link rel="alternate" href="www.example.com/jp-jp" hreflang="jp-jp"`}</code>
      </li>
      <li>
        <code>{`<link rel="alternate" type="application/atom+xml" href="posts.xml" title="Blog">`}</code>
      </li>
    </ul>
  </li>
  <li>
    <strong>
      <code>help</code>
    </strong>{' '}
    &mdash; link to help page of this document
  </li>
  <li>
    <strong>
      <code>license</code>
    </strong>{' '}
    &mdash; link to license page of this document
  </li>
  <li>
    <strong>
      <code>search</code>
    </strong>{' '}
    &mdash; link to search page of this document
  </li>
  <li>
    <strong>
      <code>next</code>
    </strong>{' '}
    &mdash; link to the next document in series
  </li>
  <li>
    <strong>
      <code>prev</code>
    </strong>{' '}
    &mdash; link to the previous document in series
  </li>
  <li>
    <strong>
      <code>pingback</code>
    </strong>{' '}
    &mdash; link to the{' '}
    <a href="https://wordpress.stackexchange.com/questions/116079/what-is-rel-pingback-and-what-is-the-use-of-this-in-my-website">
      pingback server
    </a>{' '}
    of this document (e.g., on Wordpress, if somebody writes a link to your post, Wordpress will try
    to contact the pingback server of your post to let you know that somebody has put down a link to
    your post)
  </li>
  <li>
    <strong>
      <code>manifest</code>
    </strong>{' '}
    &mdash; link to <a href="https://developer.mozilla.org/en-US/docs/Web/Manifest">PWA manifest</a>
  </li>
</ul>

<Badge color="red">Performance</Badge>
<ul>
  <li>
    <strong>
      <code>dns-prefetch</code>
    </strong>{' '}
    &mdash; tells the browser to preemptively perform DNS resolution for the target origin
  </li>
  <li>
    <strong>
      <code>preconnect</code>
    </strong>{' '}
    &mdash; tells the browser to preemptively connect to the target origin (this does DNS
    resolution, TLS negotiation, and TCP handshake)
  </li>
  <li>
    <strong>
      <code>prefetch</code>
    </strong>{' '}
    &mdash; tells the browser that it <em>should</em> fetch and cache the target resource because
    it's likely to be used in the followup navigation
  </li>
  <li>
    <strong>
      <code>preload</code>
    </strong>{' '}
    &mdash; tells the browser that it <em>must</em> fetch and cache the target resource because it's
    likely to be used in the current navigation
  </li>
  <li>
    <strong>
      <code>prerender</code>
    </strong>{' '}
    &mdash; tells the browser that it should fetch and process the target resource (e.g. fetch its
    subresources or perform rendering off-screen) because it's likely to be used in the followup
    navigation
  </li>
  <li>
    <strong>
      <code>modulepreload</code>
    </strong>{' '}
    &mdash; for JS modules: tells the browser to preemptively fetch the script (and its
    dependencies) and store it in the document's module map for later evaluation.
  </li>
</ul>

<Badge color="red">Resource type</Badge>
<ul>
  <li>
    <strong>
      <code>icon</code>
    </strong>{' '}
    &mdash; loads favicon
  </li>
  <li>
    <strong>
      <code>stylesheet</code>
    </strong>{' '}
    &mdash; loads CSS
  </li>
</ul>

### `rel` of `<a>` or `<area>`

<Badge color="red">Link to other page</Badge>
<ul>
  <li>
    <strong>
      <code>author</code>
    </strong>{' '}
    &mdash; indicates link to the author of the current document/section
  </li>
  <li>
    <strong>
      <code>alternate</code>
    </strong>{' '}
    &mdash; indicates link to the an alternative version of the current document/section
  </li>
  <li>
    <strong>
      <code>bookmark</code>
    </strong>{' '}
    &mdash; indicates permalink to the current document/section
  </li>
  <li>
    <strong>
      <code>help</code>
    </strong>{' '}
    &mdash; indicates link to the help page of the current document/section
  </li>
  <li>
    <strong>
      <code>license</code>
    </strong>{' '}
    &mdash; indicates link to the license page of the current document/section
  </li>
  <li>
    <strong>
      <code>search</code>
    </strong>{' '}
    &mdash; indicates link to the search page of the current document/section
  </li>
  <li>
    <strong>
      <code>next</code>
    </strong>{' '}
    &mdash; indicates link to the next document (in series) of the current document/section
  </li>
  <li>
    <strong>
      <code>prev</code>
    </strong>{' '}
    &mdash; indicates link to the previous document (in series) of the current document/section
  </li>
  <li>
    <strong>
      <code>tag</code>
    </strong>{' '}
    &mdash; indicates link to a tag page that applies to the current document/section (e.g. link to
    the "fiction" or "nonfiction" tag of a book)
  </li>
</ul>

<Badge color="red">Link annotation</Badge>
<ul>
  <li>
    <strong>
      <code>external</code>
    </strong>{' '}
    &mdash; indicates that the link is pointing to an external site
  </li>
  <li>
    <strong>
      <code>nofollow</code>
    </strong>{' '}
    &mdash; indicates that the author does not endorse the link (tells search engine crawlers to
    ignore the link)
  </li>
  <li>
    <strong>
      <code>noreferrer</code>
    </strong>{' '}
    &mdash; Don't include <code>Referer</code> header when navigating; behaves like{' '}
    <code>noopener</code>
  </li>
  <li>
    <strong>
      <code>noopener</code>
    </strong>{' '}
    &mdash; On the target URL, sets <code>window.opener</code> to <code>null</code> (for security)
  </li>
  <li>
    <strong>
      <code>opener</code>
    </strong>{' '}
    &mdash; On the target URL, gives access to <code>window.opener</code> (may allow{' '}
    <a href="https://mathiasbynens.github.io/rel-noopener/">phising attacks</a>)
  </li>
</ul>

### `rel` of `<form>`

<Badge color="red">Link to other page</Badge>
<ul>
  <li>
    <strong>
      <code>help</code>
    </strong>{' '}
    &mdash; ???
  </li>
  <li>
    <strong>
      <code>license</code>
    </strong>{' '}
    &mdash; ???
  </li>
  <li>
    <strong>
      <code>search</code>
    </strong>{' '}
    &mdash; ???
  </li>
  <li>
    <strong>
      <code>next</code>
    </strong>{' '}
    &mdash; ???
  </li>
  <li>
    <strong>
      <code>prev</code>
    </strong>{' '}
    &mdash; ???
  </li>
</ul>

<Badge color="red">Link annotation</Badge>
<ul>
  <li>
    <strong>
      <code>external</code>
    </strong>{' '}
    &mdash; indicates that the form is submitting to an external site
  </li>
  <li>
    <strong>
      <code>nofollow</code>
    </strong>{' '}
    &mdash; indicates that the author does not endorse the link (tells search engine crawlers to
    ignore the link)
  </li>
  <li>
    <strong>
      <code>noreferrer</code>
    </strong>{' '}
    &mdash; Don't include <code>Referer</code> header when submitting; behaves like{' '}
    <code>noopener</code>
  </li>
  <li>
    <strong>
      <code>noopener</code>
    </strong>{' '}
    &mdash; On the target URL, sets <code>window.opener</code> to <code>null</code> (for security)
  </li>
  <li>
    <strong>
      <code>opener</code>
    </strong>{' '}
    &mdash; On the target URL, gives access to <code>window.opener</code> (may allow{' '}
    <a href="https://mathiasbynens.github.io/rel-noopener/">phising attacks</a>)
  </li>
</ul>

## `sizes=[...]`

Defines: for a given screen dimension, how wide is the "slot" for the image (for example, if the image slot is 720px wide, the browser will load the smallest image from `srcset` that is at least 720px wide).

Not required if you used the "x" descriptor in `srcset`.

Sample value:

- <strong>
    <code>(min-width: 960px) 720px, 100vw</code>
  </strong> &mdash; it means that if the screen is at least 960px wide, the image slot is 720px wide.
  Otherwise, the image slot is 100% width of the screen (the definition of <code>100vw</code>).

## `srcset=[...]`

Comma-separated list of possible sources to load an image.

Use one of the following format (given as examples):

- <strong>
    <code>image-small.jpg 240w, image-medium 300w, image-large.jpg 720w</code>
  </strong>
  , where the number before "w" is the image's width (the browser will pick which one to load based on
  the value of <code>sizes</code> attribute)

- <strong>
    <code>image-small.jpg, image-medium 1.5x, image-large.jpg 2x</code>
  </strong>
  , where the number before "x" is the pixel density (defaulting to 1x if the number is not given; the
  browser will pick which one to load based on the device's pixel density)

## `target=[...]`

Targets where to open a link. Possible values:

<ul>
  <li>
    <strong>
      <code>_self</code>
    </strong>{' '}
    &mdash; current window/tab
  </li>
</ul>

<ul>
  <li>
    <strong>
      <code>_blank</code>
    </strong>{' '}
    &mdash; new window/tab
  </li>
</ul>

<ul>
  <li>
    <strong>
      <code>_parent</code>
    </strong>{' '}
    &mdash; Parent frame (otherwise behaves like <code>_self</code>)
  </li>
</ul>

<ul>
  <li>
    <strong>
      <code>_top</code>
    </strong>{' '}
    &mdash; Root frame (otherwise behaves like <code>_self</code>)
  </li>
</ul>

## References

<References
  references={[
    {
      text: 'Access-Control-Allow-Credentials (MDN)',
      link: `https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Access-Control-Allow-Credentials`,
    },
    {
      text: 'Referrer-Policy (MDN)',
      link: `https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Referrer-Policy`,
    },
    {
      text: 'HTML attribute: rel (MDN)',
      link: `https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/rel`,
    },
    {
      text: 'Referrer Policy: W3C Candidate Recommendation, 26 January 2017',
      link: `https://www.w3.org/TR/referrer-policy/`,
    },
    {
      text: 'What is rel=“pingback”, and what is the use of this in my website? (Stack Exchange)',
      link: `https://wordpress.stackexchange.com/questions/116079/what-is-rel-pingback-and-what-is-the-use-of-this-in-my-website`,
    },
    {
      text: 'Prefetch & preconnect-dns priority',
      link: `https://responsivedesign.is/articles/prefetch-preconnect-dns-priority/`,
    },
    {
      text: 'rel="tag"',
      link: `https://microformats.org/wiki/rel-tag`,
    },
    {
      text: 'HTML: Living Standard: 4.6.6 Link types',
      link: `https://html.spec.whatwg.org/multipage/links.html#linkTypes`,
    },
  ]}
></References>
