import { MetadataConfigurer } from '@pelicin/layout';
import { References, FixedColumnWidthTable, NoteBox, LightBox, Badge } from '@pelicin/components';

export const Attribute = (props) => (
  <strong>
    <code>{props.children}</code>
  </strong>
);

<MetadataConfigurer title="HTML Tags: The Basics" dateCreated="2021-01-22" />

# HTML Tags: The Basics

These are the most common HTML tags. You would put these inside `<body>`, since they correspond to the content of your web page.

- <dfn>Block-level elements</dfn> will be placed in their own line

- <dfn>Inline elements</dfn> will flow in-line with the surrounding text

<FixedColumnWidthTable
  tableStyle={{
    fontSize: '0.9em',
  }}
  headers={[
    {
      text: 'Tag',
      key: 'tag',
      formatContent: (tag) => {
        const specialMapping = {
          div: 'div-and-span',
          span: 'div-and-span',
        };
        return (
          <>
            <a href={specialMapping[tag] ? `#${specialMapping[tag]}` : `#${tag}`}>
              <code>&lt;{tag}&gt;</code>
            </a>
          </>
        );
      },
      width: 100,
    },
    {
      text: 'Type',
      key: 'type',
      formatContent: (type) => {
        return type === 'block' ? (
          <Badge color="red">Block</Badge>
        ) : type === 'inline' ? (
          <Badge color="blue">Inline</Badge>
        ) : (
          <Badge color="gray">Other</Badge>
        );
      },
      width: 80,
    },
    {
      text: 'Description',
      key: 'description',
    },
  ]}
  contents={[
    {
      tag: 'div',
      type: 'block',
      description: <>Used to create generic block-level container</>,
    },
    {
      tag: 'span',
      type: 'inline',
      description: <>Used to create generic inline container</>,
    },
    {
      tag: 'h1-h6',
      type: 'block',
      description: <>Used to create headers</>,
    },
    {
      tag: 'p',
      type: 'block',
      description: <>Used to create paragraphs</>,
    },
    {
      tag: 'hr',
      type: 'block',
      description: <>Used to create horizontal ruler</>,
    },
    {
      tag: 'a',
      type: 'inline',
      description: (
        <>
          Used to create <a href="#">anchors/hyperlinks</a>
        </>
      ),
    },
    {
      tag: 'b',
      type: 'inline',
      description: (
        <>
          Make text <b>bold</b>
        </>
      ),
    },
    {
      tag: 'i',
      type: 'inline',
      description: (
        <>
          Make text <i>italic</i>
        </>
      ),
    },
    {
      tag: 'u',
      type: 'inline',
      description: (
        <>
          Make text <u>underlined</u>
        </>
      ),
    },
    {
      tag: 's',
      type: 'inline',
      description: (
        <>
          Render text with <s>strikethrough</s>
        </>
      ),
    },
    {
      tag: 'sub',
      type: 'inline',
      description: (
        <>
          Render text as <sub>subscript</sub>
        </>
      ),
    },
    {
      tag: 'sup',
      type: 'inline',
      description: (
        <>
          Render text as <sup>superscript</sup>
        </>
      ),
    },
  ]}
/>

## `<div>` and `<span>`

`<div>` is used to create generic <dfn>block-level container</dfn>, which means it will take the whole horizontal space of its parent
(it will be on its new line).

`<span>` is used to create generic <dfn>inline container</dfn>, which means it will flow with the current line of text.

These containers by themselves will not change how the content is displayed; you will normally use this for layouting and styling with CSS.

```html
<!-- div creates its own line -->
<div style="background-color: red; color: white">
  I'm creating a new line!
</div>

<!-- span flows inline with the surrounding text -->
Lorem ipsum
<span style="background-color: blue; color: white">
  I'm flowing with the text
</span>
dolor sit amet
```

<LightBox label="Preview">
  <div style={{ backgroundColor: 'red', color: 'white' }}>I'm creating a new line!</div>
  Lorem ipsum <span style={{ backgroundColor: 'blue', color: 'white' }}>
    I'm flowing with the text
  </span> dolor sit amet
</LightBox>

## `<h1>` &ndash; `<h6>`

These are used to create <em>headers</em>. They are block-level elements.

- `<h1>` would normally be used for title
- `<h2>` would be for the subtitle
- `<h3>` would be for the sub-subtitle
- so on and so forth.

```html
<h1>H1: Nice title</h1>
<h2>H2: Nice sub-title</h2>
<h3>H3: Nice sub-sub-title</h3>
```

<LightBox label="Preview">
  <h1 style={{ border: 'none', margin: 0, marginBottom: 10, padding: 0 }}>H1: Nice title</h1>
  <h2 style={{ border: 'none', margin: 0, marginBottom: 10, padding: 0 }}>H2: Nice sub-title</h2>
  <h3 style={{ border: 'none', margin: 0, padding: 0 }}>H3: Nice sub-sub-title</h3>
</LightBox>

## `<p>`

Marks a paragraph. It's a block-level element.

<!-- prettier-ignore -->
```html
<p>Lorem ipsum dolor sit amet. Consectetur adipisicing elit.</p>
<p>Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.</p>
```

<LightBox label="Preview">
  <p>Lorem ipsum dolor sit amet. Consectetur adipisicing elit.</p>
  <p>Sed do eiusmod tempor incididunt ut labore et dolore magna aliqua.</p>
</LightBox>

## `<hr>`

Creates <dfn>horizontal ruler</dfn>.

<!-- prettier-ignore -->
```html
one two three
<hr>
one two three
```

<LightBox label="Preview">
  one two three
  <hr />
  one two three
</LightBox>

## `<a>`

Creates <dfn>anchors</dfn> and hyperlinks.

<Badge color="blue">Possible attributes</Badge>
<ul>
  <li>
    <Attribute>href=[...]</Attribute> &mdash; where the link points to
  </li>
  <li>
    <Attribute>target=[...]</Attribute> &mdash; where to open it (
    <a href="/web/html/html-attributes-recurring#target">read here</a>)
  </li>
  <li>
    <Attribute>download=[...]</Attribute> &mdash; instead of opening the link, prompt the user to
    download it. If value is not empty, it will be the name of the downloaded file. (Only works for
    same-origin URLs, or <code>blob:</code> and <code>data:</code> schemes)
  </li>
</ul>

```html
<p>To search the internet, <a href="https://google.com">click here</a>!</p>

<!-- Prompts download when clicked -->
<p>
  Taken from MDN site:
  <a href="/res/web/html/mdx-info2.png" download>Download an image?</a>
</p>

<!-- When clicked, the header will be brought to top of page -->
<h1 id="my-anchor">My Anchor</h1>
<p>Click <a href="#my-anchor">here</a> to go to My Anchor</p>
```

<!-- prettier-ignore -->
<LightBox label="Preview">
  <p>To search the internet, <a href="https://google.com" target="_blank">click here</a>!</p>
  <p>Taken from MDN site: <a href="/site/skulls.png" download>Download an image?</a></p>
  <h1 style={{ border: 'none', margin: '16px 0 0', padding: 0, }} id="my-anchor">My Anchor</h1>
  <p>Click <a href="#my-anchor">here</a> to go to my-anchor</p>
</LightBox>

## `<b>`

Makes text <em>bold</em>.

```html
<b>I'm bold!</b>
```

<LightBox label="Preview">
  <b>I'm bold!</b>
</LightBox>

## `<i>`

Makes text <em>italic</em>.

```html
<i>I'm italic!</i>
```

<LightBox label="Preview">
  <i>I'm italic!</i>
</LightBox>

## `<u>`

Makes text <em>underlined</em>.

```html
<u>I'm underlined!</u>
```

<LightBox label="Preview">
  <u>I'm underlined!</u>
</LightBox>

## `<s>`

Renders text with <em>strikethrough</em>.

```html
<s>I'm struck-through!</s>
```

<LightBox label="Preview">
  <s>I'm struck-through!</s>
</LightBox>

## `<sub>`

Renders text as <em>subscript</em>

```html
I'm a <sub>subscript</sub>
```

<LightBox label="Preview">
  I'm a <sub>subscript</sub>
</LightBox>

## `<sup>`

Renders text as <em>superscript</em>

```html
I'm a <sup>superscript</sup>
```

<LightBox label="Preview">
  I'm a <sup>superscript</sup>
</LightBox>

## References

<References
  references={[
    {
      text: 'HTML elements reference (MDN)',
      link: `https://developer.mozilla.org/en-US/docs/Web/HTML/Element`,
    },
  ]}
></References>
